#include <stdio.h>
#include <stdlib.h>

int m;

void v()
{
    char buffer[520];

    fgets(buffer, 0x200, stdin);
    printf(buffer);

    if (m == 64)
    {
        fwrite("Wait what?!\n", 1, 12, stdout);
        system("/bin/sh");
    }
}

int main()
{
    v();
    return 0;
}